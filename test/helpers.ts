export const singleArg = ({ a, b }: { a: number; b: number }): number => a + b;

export const multipleArgs = (a: number, b: number): number => a + b;

export const multipleArgsLongLine = (
  a: number,
  b: number,
  c: number,
  d: number,
): number => a + b + c + d;
