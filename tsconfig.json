{
  // When editing this repo, this line will be flagged by TS
  // However for downstream users this IS the correct path
  "extends": "../../@actinc/eslint-config/tsconfig.json",
  "ts-node": {
    "transpileOnly": false,
    "compilerOptions": {
      "module": "commonjs",
      "useUnknownInCatchVariables": true
    }
  }
}
